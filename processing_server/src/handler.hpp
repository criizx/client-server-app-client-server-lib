#pragma once

#include <string>
#include <vector>

namespace handler {

std::string format_data(const std::string &input);

std::vector<char> format_data_wrapper(const std::vector<char> &input);

} // namespace handler
